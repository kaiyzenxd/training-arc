# --- Secrets: never commit these ---
gitrepocreation.txt
.env
.env.*
!.env.example
*.key
.mcp.json
secrets/

# --- Node / Next.js (auto-generated, huge) ---
node_modules/
.next/
out/
build/
dist/
.vercel/
*.tsbuildinfo
next-env.d.ts

# --- Logs ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- OS / editor junk ---
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# --- n8n local data (if you ever copy it in) ---
.n8n/

# --- Agent tooling: installed locally, kept out of git ---
# Skills/agents live on disk and keep working; they're just not versioned here.
# Restore skills installed via `npx skills` with:  npx skills install
.agents/
.codex/
.cursor/
.claude/skills/
.claude/agents/
.claude/plugins/
skills-lock.json

# impeccable working files (design contracts, review captures) — local only
.impeccable/

# Session scratch / handoff notes
RESUME.md

# auto-generated by `next dev` (Next.js agent boilerplate — regenerates)
Website/AGENTS.md
Website/CLAUDE.md
